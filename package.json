{
  "name": "suggest-edit-function",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "registry:bundle": "node scripts/bundle-registry.mjs",
    "vercel-build": "node scripts/bundle-registry.mjs && npm test"
  }
}
